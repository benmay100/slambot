# ekf.yaml
ekf_filter_node:
    ros__parameters:
        use_sim_time: true
        frequency: 30.0
        
        two_d_mode: true

        odom_frame: slambot/odom
        base_link_frame: slambot/base_footprint
        world_frame: slambot/odom

        odom0: /odom
        odom0_config: [true,  true,  false,
                       false, false, true,
                       false, false, false,
                       false, false, false,
                       false, false, false]
        odom0_differential: false
